so, in weird database science 

  tables are relations
  columns are attributes
  rows are tuples
  domains are types

I wonder, how many time (of explanation of concept) would be saved, if industry would use UNKNOWN instead of NULL

"Compositional" - means, with ability to run queries against queries
